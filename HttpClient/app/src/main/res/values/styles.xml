<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyTheme" parent="@android:style/Theme.DeviceDefault.Light">
        <!-- Customize your theme here. -->
        <item name="android:actionBarStyle">@style/myActionBarStyle</item>
        <item name="android:actionBarSize">50dp</item>
    </style>

    <style name="myActionBarStyle" parent="@android:style/Widget.Holo.Light.ActionBar">
        <!-- Customize your theme here. -->
        <item name="android:background">@color/colorTopicBlue</item>
        <item name="android:titleTextStyle">@style/myTitleTextStyle</item>
        <item name="android:actionOverflowButtonStyle">@style/myActionButtonStyle</item>
    </style>

    <style name="myActionButtonStyle" parent="@style/Widget.AppCompat.Light.ActionButton.Overflow">
        <item name="android:src">@drawable/choose</item>
    </style>
    
    <style name="myRadioButton">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:button">@null</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">@color/radio_button</item>
    </style>

    <style name="myTitleTextStyle">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/colorWhite</item>
    </style>

</resources>
